import React, { Component } from 'react'
import {
    Form,
    FormGroup,
    Label,
    Button,
    Input } from 'reactstrap';
import shortid from 'shortid';

import { connect } from 'react-redux';
import { addPlace } from '../actions';

export default class SpaceRegistration extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}
